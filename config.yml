title: SMO-iris
description: '201800171080'
template:
  repo: smo-iris-template
  name: smo-iris
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
